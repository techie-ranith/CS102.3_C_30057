#include <stdlib.h>
#include <stdio.h>

int main(){
    int n1,n2;
    printf("Enter two numbers:");
    scanf("%d%d",&n1,&n2);
    if (n1>n2)
    {
        printf("first number is the heighest number");
    }else{
        printf("second number is the heighest number");
    }
}
